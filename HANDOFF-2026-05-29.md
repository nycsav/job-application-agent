# Handoff — Career / Job workstream (2026-05-29)

Plain-language note so the next thread picks up fast. No re-explaining needed.

---

## The big idea
Keep Sav's email inbox clean. Move all job/contract noise into Claude. Stay quiet by default — only ping Sav when there's a decision to make or something broke.

**Quiet by default. Loud only when you're needed.**

---

## Done already
- **The one page (dashboard) is built** — "Career Command Center" artifact. Opens in the Cowork sidebar, pulls the last 7 days of job/contract emails (Ladders, LinkedIn, Indeed, Dice) fresh each time. Has a "Needs you" card and an "Alerts" card up top. Client work is NOT shown.
- **Guardrail locked in memory** — client systems are off-limits.

---

## The plan (what we're building)
A simple notification flow that lives in Claude, not the inbox:
1. **One page** to check in Claude (done).
2. **One short summary a day** to Sav's personal Gmail — not an email per application.
3. **Only ping Sav** when a job needs a decision, or when something breaks.

---

## Next steps — in order
1. ~~Stand up the dashboard~~ ✅ done
2. **Add 3 small files** for logging/queues: applications-log, ACTION-QUEUE (needs-you), ALERTS (broke). Additive, zero risk.
3. **Point the job briefing to personal Gmail** (one daily digest) instead of sav@ensopartners. Small edit.
4. **Connect Indeed + Dice directly** (they already have live MCP connectors) instead of fragile email-scraping. Small edit, fewer breakages.
5. **Add Ladders as a source to the existing `job-scan-morning` task** (Sav confirmed 2026-05-29 — do this BEFORE tomorrow's run, ~11am ET).
   - Read Ladders emails via the **Gmail MCP** on the business inbox (sav@ensopartners) — query: `from:theladders.com newer_than:7d` (matches + Apply4Me confirmations). No relay hack needed.
   - Extract real listings, run the SAME scoring + 4-way resume routing + apply path as LinkedIn.
   - **CRITICAL DEDUP — must ship together:** before applying, read the Ladders **Apply4Me confirmation** emails and skip any role Ladders already applied to. Never double-apply (employer-facing reputation risk).
   - Apply path unchanged: LinkedIn Easy Apply = auto; company-site/ATS = queue to ACTION-QUEUE (needs Sav).
   - Output rolls into the ONE daily personal-Gmail digest. Business inbox auto-files Ladders into folders (Matches / Applied) and auto-archives promo.
   - NOTE: this is the "Claude applies" path for high-value roles; Ladders' own Apply4Me keeps handling the simple ones. Both coexist.
   - ✅ TESTED 2026-05-29 (read-only dry run): Gmail MCP reads business inbox cleanly; dedup correctly caught the Dell Apply4Me application as already-done; scoring ran (4 of 10 qualify: AlixPartners 9, Pear-NYC 9, AHT 8, Starbridge 7). Only the apply-click step remains untested. Tomorrow = just wire in the apply step + verify Easy-Apply-vs-company-form per role.
6. **Add a daily "guardian" check** that updates the dashboard and pings only on a decision or a failure.
7. **Tidy duplicates** (last, most care): two `job-application-agent` copies → one; two "Professional: Jobs & Resumes" folders → one source of truth; the dormant `notion-career-agent` → retire or fold in.

How each step goes: **I show it → Sav approves → I apply → I verify.** One at a time.

---

## Key facts / routing decisions
- **Three lanes:** personal job search = sav.banerjee@gmail.com (LinkedIn alerts via Gmail scan — no LinkedIn connector exists). Contract/advisory work = business email (Ladders on sav@ensopartners). Output digest = personal Gmail.
- **Indeed & Dice** already have live MCP connectors (no scraping needed). **LinkedIn** has none — stays on Gmail scan.
- The live job engine is the `job-scan-morning` scheduled task (runs weekday mornings, writes to the Google Sheet tracker).

---

## Ground rules (how to work with Sav)
- **Client work is off-limits** — Heller, Eligard, Rubraca, Eton, the Google Ads optimizer, client email triage. Never edit. It's the income.
- **Sav is not a developer** — no commands, no code choices. Plain language, click-level only.
- **Visuals = Wieden+Kennedy strategy style** — one big idea, lots of space, almost no words, max 3 pillars. No dense flowcharts, no legends, no jargon.
- **Colorblind** — white/light text, never red/green; use blue + amber, and always label with words (not just color).
- **Don't loop on proposals.** Decide and act on the safe stuff; show before touching anything live.
- **Keep threads short** — checkpoint to a handoff like this before context gets bloated, then start fresh.

---

## Staged tonight (2026-05-29) — ready for morning review
4 application packets drafted by parallel subagents, saved in `staged-applications/2026-05-29/`:
- `alixpartners-principal-ai-director.md` — MD/Managed Services resume · letter done
- `pear-founding-applied-ai-lead.md` — Forward-Deployed resume · letter done
- `aht-principal-ai-architect.md` — Default v3 resume · letter done · FLAG: no insurance background
- `starbridge-founding-fde-tech-lead.md` — Forward-Deployed resume · letter done · FLAG: leans hands-on SWE

**Each needs from Sav before sending:** (1) one-line "why this company", (2) a salary number. ~5 min total.
**Verify at send:** resume PDFs exist in `materials/resumes/` (confirmed present May 27 via Desktop Commander; subagents couldn't see them from their sandbox — non-issue). Pull each live JD to tune ATS keywords before submit.

## MVP for tomorrow morning (in order)
1. Review the 4 staged packets, add the 2 missing inputs each.
2. Apply: LinkedIn Easy Apply → auto; company ATS forms → drive-to-last-click, Sav approves send.
3. Then wire Ladders into job-scan-morning + the Apply4Me dedup (already tested read-only).
4. Flagship build: "ATS Autopilot — tailored to the last click, human only at the gates."

## Where to start the next thread
Open a fresh thread and say: *"Review the 4 staged Ladders applications and let's send them."*
